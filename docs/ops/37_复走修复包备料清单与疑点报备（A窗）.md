# 复走修复包 · 备料清单与疑点报备（A 窗）

> 编制：Kimi Code A 窗（施工方，角色卡⑧ V2.1）｜2026-09-23
> **这份发给谁：老板（审）+ 产品侧（冻结任务书输入）+ 项目经理（登记）**。
> 性质：**只读备料产物，非施工**——发令枪= R11a 双签 + 产品侧冻结任务书 + 施工令 MD 推仓（铁律：无冻结任务书不施工）。
> 依据档案：12 表 V3.3 位 3（范围唯一锚）／24 号档（PR38+R12 走查）／32 号档（R11a 走查）／35 号档（P1 交底单）／20 号档+26 号档（R12+权限矩阵 V1.3）／09 看板 D-2。
> 侦察基线：本地仓 main（代码面=生产部署 main@65911fba，已核 `git diff 65911fba HEAD -- server apps packages` 零差异；其后提交全为文档流）。所有「现状」均带 文件:行号 实证锚，无推测项。

---

## 一、范围总表（12 表 V3.3 位 3，逐项对勘结果）

| # | 修复件 | 出处 | 侦察结论一句话 | 改动面 |
|---|---|---|---|---|
| A1 | 差评链路修复：默认 5 星→0+未选禁提交+星钮≥44px+e2e 断言 | 12 表/24 档 | **双评价入口并存**：live 页 ReviewPanel 全合规；预约详情页内联表单三处全踩线，且评价推送通知链接恰好落在缺陷入口 | 客户端 1~2 文件 + 新增 e2e 脚本 |
| A2 | 星值三处一致 | 24 档 P2-1 | 代码面两表同事务同值写入，**不可能数据分叉**；根因=A1 的默认 5 星（客户以为点了 2 星，实际提交默认值）——治 A1 即治 A2 | 随 A1，零额外改动 |
| A3 | 步名归一「交付检查」 | 24 档 P2-2/冻结墙 | 「前后对比照」散布 **6 处代码+2 处硬编码报错文案**；另发现通知套还有两个冻结表外异名（术前检查/家长确认）、步 1 简称差 | shared+server+三端 6 文件 |
| A4 | XP 分值核查 | 24 档 P3 | **非计分错误**：寄养 3 晚×2 分合成一行「完成服务 +6」，标注粒度不足（source 枚举不分洗护/寄养晚） | 员工端 XpPage+server 出参 |
| A5 | 差评 −8 链路修复 | 24 档 P1-2 | 代码分支存在且有 e2e 实证；未触发的最可疑门径=**未指派员工的单整段跳过**（寄养单 create 不派单→永远无 −8）；历史单无回填通道 | server appointment.ts（待裁定） |
| A6 | 好评单号链接 | 24 档 P2-3 | 数据通道已通（xp_events.source_id 已出参），UI 未渲染；review 类挂的是 reviewId 需多跳一层解析 appointmentId | server xp.ts+员工端 XpPage |
| A7 | 提示文案区分「非安全源/权限被拒」 | 24 档 P1-1 | 打卡文案实锤误导（http 下 geolocation 恒回 PERMISSION_DENIED）；全仓 `isSecureContext` 零命中；扫码侧已隐式区分但不同套 | 员工端 2 文件+共享 helper |
| A8 | 扣减录入三端前端入口 | 12 表 | server 端点齐备（createDeduction，含 50% 硬闸门+余量透出）；**三端前端零入口**（全仓 grep 零命中） | 商家端或员工端店长视图（待裁定归属） |
| A9 | 差评 −8 分值可配行核查 | 12 表 | **已可配**：xp_rules 种子 −8，配置端口 XP 页签可改（全表唯一允许负分值的键）——本件=核查确认+复走清单加一条实测现值 | 零代码 |
| B1 | 退款阈值前端页签 | 12 表/32 档§六 | server 管道已通（configRules 第五域 refund 开箱可用，refund_rules 表+种子 50000 在库）；**纯前端缺口**：页签数组+字段元 | 商家端 RulesConfigPage 单文件 |
| B2 | 0015 种子行 | 12 表 | 「7 行缺行」说**不成立**（种子 7 行与读侧全对上）；真缺口=0015 迁移纯 DDL 无种子，fresh 库 `drizzle migrate` 后会员档为空 | 补幂等迁移种子 or 定向补种子脚本（待裁定） |
| B3 | 驳回权放开店长 | 12 表/26 档 V1.3 | server rejectDraft=仅店主硬闸、前端驳回钮 `isOwner` 闸门；强制原因+留痕+本店校验已达标；**但 draft 单当前无产生入口**（超阈值=FORBIDDEN 硬拒不落 draft）——见疑点 Y5 | server refund.ts 一行级+前端闸门 |
| C1 | F1 smoke 环境口径（D-2） | 09 看板 | `smoke-routes.mjs:55` 默认 SMOKE_APPT_ID=历史演示单 ULID，fresh seed 库不存在→`/execute` 两红机制已坐实（NOT_FOUND 文案不在 anchors 内）；**`scripts/` 无 README**，载体需新建 | 脚本头注释修正+新建 scripts/README.md |
| C2 | 冒烟寄养单跑完即取消释放槽 | 12 表/32 档 O2 | smoke-deploy 全脚本**无清理段**：寄养单每跑占 3 晚槽位永占（capacity=2，约 2 次复跑即满员）；5.15(d) held 单不 void（对照组写法仓内已有样例） | smoke-deploy.mjs 尾部补 cancel/voidBill |
| D1 | 年费分摊双口径页面落地（R11a P1-1） | 35 档/32 档⑦ | server `membership.amortizationStats` 已上线返回正确（cashFen/amortizedFen），**前端消费方零命中**——纯前端接线 | 商家端日结页+dashboard 卡 |
| D2 | 安心包售卖入口关停（R11a P1-2） | 35 档/32 档 | 应急下架已由运营零代码完成（三件 off）；代码层=收银台+商城两出口均未排除 `category='care_package'`（收银台出口连 status 都不过滤）；独立库存域随 C2 批不在本包 | server mall.ts 2 处 or 前端过滤（取舍见 Y8） |

---

## 二、逐项详录（实证锚）

### A1+A2 差评链路/星值一致（一治俱治）
- 合规件：`apps/customer/src/components/live/ReviewPanel.tsx`（默认 0 星 :65／星钮 44×44 :43／`rating===0` 禁提交 :120）。
- 缺陷入：`apps/customer/src/pages/AppointmentDetailPage.tsx`——默认 `useState(5)` **:131**；私有 StarRating 星钮 28×28 无触控类 **:77-96**；提交钮无 rating gating **:798-799**。
- 放大器：评价推送 `review.prompt` 链接=`/appointments/{id}`（`server/src/index.ts:201`、`server/src/routers/boarding.ts:346`）——通知触达恰好落在缺陷入口。
- e2e 落点：无 Playwright（零新依赖红线），仿 `apps/customer/scripts/me-page-e2e.mjs` 新增 `review-e2e.mjs`（CDP+`data-testid`/`aria-label="${n} 星"` 选素现成）；夹具需 completed 未评价预约（见疑点 Y11 执行顺序）。

### A3 步名归一
- 冻结表：消毒工具确认/预检/洗护/精修/**交付检查**/完成确认（CJ-0919-01）。
- 「前后对比照」6 处：`packages/shared/src/constants/steps.ts:41`／`apps/staff/.../ServiceCard.tsx:22`／`server/src/routers/serviceStep.ts:96`（连带客户端相册分组名复用）／`packages/shared/src/constants/events.ts:90`+`server/src/realtime/events.ts:105`（通知套）／`apps/merchant/.../appt-utils.ts:122`；硬编码报错文案 2 处：`serviceStep.ts:268/:409`。
- **新发现**：通知套另有异名「术前检查」（冻结表=预检）、「家长确认」（冻结表=完成确认）；步 1 执行页显示简称「消毒」vs 冻结「消毒工具确认」；客户端 live 页实际显示共享全称（含「前后对比照」）+`AppointmentLivePage.tsx:83` 文案「正在拍摄前后对比照」。
- 同屏撕裂：执行页步名行用简称套、打标提示用全称套（ExecuteStepper.tsx:194 vs ExecutePage.tsx:229/:422）。

### A4 XP 分值（P3 真相）
- 「完成服务 +6」= 寄养 3 晚×`xp_service_boarding_night`(+2/晚) 合成一行（`server/src/routers/boarding.ts:322-335`），与规则页「完成一单服务 +2/单」并排展示致误读。**计分无误，标注粒度不足**。
- 落点：`xp.myEvents` 出参补规则键/晚数（`server/src/routers/xp.ts:189-225`）+`XpPage.tsx:17-25` 标签映射按 sourceId 形态（`boarding:` 前缀）区分渲染；根治需 xp_events 加 ruleKey 维度（涉 schema，见 Y-备）。
- 伴生报备：洗护 +2 走 60s 补偿任务而非完成事务（`server/src/index.ts:186-248`，R10 时已报备），到账有延迟——是否随批根治待裁定。

### A5 差评 −8 未触发（P1-2 根因定位）
- 分支存在：`server/src/routers/appointment.ts:1930-1949`（rating≤2→`xp_penalty_low_star` −8），e2e 实证 `server/src/__tests__/e2e.ts:1445-1456`。
- 三个跳过门径：①`appointment.ts:1905` `if (appt.staffId)`——**未指派员工的单整段跳过 reviews 行与 XP**；寄养单 create 不派单（`appointment.ts:1040` staffId=NULL）、核销不改写——**寄养单差评永远无 −8**；②`appointment.ts:1911` staff 记录缺失静默跳过；③review/penalty 无回填通道（补偿任务只补 service 源 `index.ts:222-243`）——R10 上线前已评价的单永远无 −8。
- **待产品裁定**：寄养/未指派单差评归属口径（寄养有退房核销人可作归属）；历史单是否补账。

### A6 好评单号链接
- `xp_events.source_id` 已随 `xp.myEvents` 出参（`xp.ts:204`）；service 行=appointmentId，review/penalty 行=reviewId（需联 `reviews.appointmentId` 多跳一层），boarding 行=`boarding:{stayId}` 需剥前缀。
- 落点：`xp.ts myEvents` 出参解析单号 + `XpPage.tsx:246-269` 事件行渲染单号/链接。纯读侧+UI。

### A7 提示文案区分
- 打卡：`apps/staff/src/pages/staff2/AttendancePage.tsx:152-172`——无 `isSecureContext` 前置分支；http 下 `navigator.geolocation` 存在但恒回 PERMISSION_DENIED→落「权限被拒」误导文案。
- 扫码：`apps/staff/src/components/scan/QrScanner.tsx:100-122`——已隐式区分（mediaDevices 缺失→HTTPS 文案/NotAllowedError→权限文案），但与打卡侧不同套。
- 落点：共享 helper（`apps/staff/src/lib/`）判 `window.isSecureContext===false`→专用「非安全源」文案，两处同套。

### A8 扣减录入前端入口
- server：`server/src/routers/commission.ts:1223-1279` `createDeduction`（owner|manager，50% 硬闸门+返回 `monthUsedFen/monthCapFen` 余量）。
- 前端：三端 grep `createDeduction` 零命中；员工端 PayPage 只读展示扣减记录。
- 落点（归属待裁定）：商家端 FinancePage/员工管理页加扣减对话框，或员工端店长视图 ManagerPage 加区块——调既有端点，server 零改动。

### B1 退款阈值页签
- server：`configRules.ts:31` domain 枚举已含 `'refund'`→`refund_rules` 表（0014 迁移建表+种子 `seed.ts:373` threshold_fen=50000）；list/save/versions 三端点开箱可用。
- 前端缺口：`RulesConfigPage.tsx:44-51` DOMAIN_TABS 无 refund 项；`:83-111` FIELD_META 无 threshold_fen（不补会以「分」裸露）。
- 落点：单文件——联合类型+DOMAIN_TABS+DOMAIN_TITLE+FIELD_META（conv:'yuan'）+DOMAIN_NOTICE 小字。server 零改动。

### B2 0015 种子行
- 事实：`server/drizzle/0015_unusual_ares.sql`（81 行）纯 DDL 五表，**无 INSERT**；会员档 7 行种子在 `server/src/db/seed.ts:392-400`，与配置端口/读侧键名逐一对上，**无缺行**。
- 真缺口：`db:seed` 是清空式全量重建（`seed.ts:30-92` CLEAR_ORDER），生产不可跑；纯 migrate 的新库 member_plans 为空→plans 端点空档/页签空列表。
- 落点（待裁定）：新增 0016 幂等种子 INSERT（7 行照 seed 数值），或定向「只补配置不清库」脚本。

### B3 驳回权放开店长（V1.3）
- server：`refund.ts:1241` `rejectDraft: merchantOwnerProcedure`（仅店主）；强制原因（:1242）/本店校验（:1250）/留痕（:1257-1267，复用 settle_note）/事件 emit 全达标。
- 前端：`CashierRefundsPage.tsx:310` 驳回钮闸门 `r.status==='draft' && role.isOwner`。
- **关键发现**：draft 单无产生入口——execute 恒落 executed（`:910`），超阈值/涉储值=FORBIDDEN 硬拒（`:31-34` 头注自承），membership.cancel 也直落 executed。放宽驳回闸门后**仍无可驳之单**。见疑点 Y5。

### C1 F1 口径
- `scripts/smoke-routes.mjs:55` 默认 ULID `01M256D240E19GWNG2QMFV3Q8V` 全仓仅两脚本命中、seed 不造预约——指向生产规范环境历史演示单；fresh 库 NOT_FOUND→员工端渲染「预约不存在」不在 anchors（['第','步','核销','无法执行该预约']）→`/execute` 两轮同红。
- `scripts/` 目录无 README.md（6 个 .mjs）——F1 载体需新建 `scripts/README.md`（三闸门合写）+修正 `smoke-routes.mjs:20-28/:54-55` 注释旧口径。

### C2 冒烟残留清理
- 寄养：`smoke-deploy.mjs:643-692`（§5.14c）造 3 晚单→checkout completed **不释放槽**；`appointment.ts:428-466` 占槽/`:473-524` 释放函数存在但 checkout 不调用→每跑 bookedCount 永 +3（标准间 capacity=2）。
- 挂单：`smoke-deploy.mjs:761-775`（§5.15d）hold 服务行单验证 403 后**无 voidBill 收尾**（每跑残留 1 张 held 单，金额为 88 折服务价非 ¥0——¥0 挂单来源见疑点 Y10）。
- 仓内已有「造单后取消」写法样例：`scripts/e2e-nav-check.mjs:258-262`。

### D1 年费分摊双口径上屏
- server：`membership.amortizationStats`（`server/src/routers/membership.ts:721-755`）`input{month:'YYYY-MM'}`→`{month,cashFen,amortizedFen}`，merchantProcedure；32 号档实测数据正确。
- 前端：全 apps grep `amortizationStats` **零命中**。
- 落点：日结页 `apps/merchant/src/components/cashier/DayClosePanels.tsx`（DayCloseForm 分列区 :136-177 下加「其中：会员费分摊确认（本月累计）¥X」**参考口径行，不入任何合计**）+dashboard `StatCards.tsx:88-96` 营业额卡口径注；查询失效联动挂 CashierBillSettled（售卡已 emit，`membership.ts:220`）。
- **附带发现**：amortizedFen 不按店过滤（`membership.ts:746-749` 全表 active）——会员卡三店通用所致，单店看板显示的是全连锁口径。见疑点 Y7。

### D2 安心包售卖入口关停（代码层）
- 收银台源：`CashierPage.tsx:113-116`→`mall.listProductsForStore`（`server/src/routers/mall.ts:322-355`）——只过滤 storeId，**不按 status、不排除 care_package**；结账内核 `cashier.ts:281-284` 不拦。
- 客户端商城：`MallPage.tsx:117-119`→`mall.listProducts`（`mall.ts:214-250`）——过滤 status='on' 但**不排除 care_package**。
- 取舍点：`listProductsForStore` 被收银台开单与商品管理页（ProductsPage）**共用**——server 侧直接过滤会影响管理端可见性。见疑点 Y8。

---

## 三、疑点报备（冻结前须裁定，编号便于回指）

| # | 疑点 | 需要谁拍 | 建议 |
|---|---|---|---|
| Y1 | **评价双入口去留**：详情页内联表单（缺陷入）vs ReviewPanel（合规件）；评价通知链接现落详情页 | 产品侧 | 建议删内联表单、详情页评价区复用 ReviewPanel（单入口单口径，永绝 A2 类问题） |
| Y2 | **差评 −8 归属口径**：寄养单/未指派单差评扣谁？历史单（R10 前已评价）是否补账？ | 产品侧+老板 | 建议：寄养单归属=退房核销人（boarding.checkout 已有发 XP 给核销人的先例）；历史不补账（台账留痕说明即可） |
| Y3 | **步名归一范围**：仅步 5「前后对比照→交付检查」，还是连通知套异名（术前检查/家长确认）+步 1 简称+客户端 live 页显示名一把归一？ | 产品侧 | 建议一把归一（异名留着就是下一个走查缺陷）；客户端向文案随改 |
| Y4 | **六步↔七节点照片数映射表**：到手照 1 无落点、消毒 3 vs 步1 min1、洗护 2 vs min3、交付细节 3 vs 步4 min2——非文案问题，需映射裁定表 | 产品侧+QA | 建议产品侧出映射裁定表入冻结任务书，施工照表改三处拷贝+smoke 断言（`appointment.smoke.ts:594` 会随动）；注意 `required_photos` 为核销时快照，不回溯在途单 |
| Y5 | **驳回权（B3）是否连带「超阈值/涉储值落 draft 待批」新流程**：现超阈值=FORBIDDEN 硬拒不落 draft；V1.3 店长驳回权无 draft 可驳。只放宽驳回闸门=空转 | 产品侧+老板 | 建议任务书写死二选一：①本批只放宽闸门（等 draft 产生机制后续批）；②同批改 execute 链路（超阈值落 draft→店主批/店长驳）——注意②动 R12 钱链路，工程量与风险显著上升 |
| Y6 | **0015 种子行（B2）口径**：补幂等迁移种子（0016）还是定向补种子脚本？ | 产品侧 | 建议 0016 幂等 INSERT（IF NOT EXISTS 口径），与迁移同源可追 |
| Y7 | **amortizedFen 跨店口径**（D1 附带）：单店日结/看板的分摊额=全连锁合计还是本店（按 sold_store_id）？ | 产品侧 | 建议本店口径按 sold_store_id（办卡店归属），与「哪家店会卖卡」报表维度同构 |
| Y8 | **care_package 过滤取舍**（D2）：server 过滤会影响商品管理页可见性 | 产品侧 | 建议 server 端点加入参开关（默认排除、管理端显式含）或收银台/商城前端各自过滤——推荐前者（结构性防复活，与 35 号档「防误上架」同源） |
| Y9 | **32 号档 P2 群是否搭车入包**：识别条回馈金余额（P2-1：已落，无需动）/有效期年份（P2-2：MemberSearch.tsx:188+MembershipPanel.tsx:302 两处补年份）/会员档页签三瑕疵（P2-3 控件串味 buildModel:549-561、P2-4 警示文案错域 :1362-1363、P2-5 说明文案硬编码+365 天三处硬编码 MemberOpenPage.tsx:140,202/MembershipPanel.tsx:157,174,486-487） | 产品侧 | 建议 P2-2/P2-3/P2-4/P2-5 搭车（全是小改且同在 RulesConfigPage/收银台两文件区）；P2-1 已落实测无需修 |
| Y10 | **挂单队列 3 张 ¥0 挂单来源未坐实**：代码面确证的残留路径（5.15d held 单）金额非 ¥0；需以现库 `cashier_bills status='held'` 取证 | 产品侧（取证） | 修复包收口前请产品侧给现库取证结论；清理动作（取消释放槽）照 C2 先行 |
| Y11 | **差评 e2e 夹具执行顺序**：fresh 环境造 completed 未评价单链路长；是否接受 smoke-deploy 先跑作为前置写进施工令 | 产品侧 | 建议施工令写明顺序：smoke-deploy→review-e2e（复用其造单产物） |
| Y12 | **闸门命令面聚合**：smoke/typecheck 无根 package.json script（散在 README/DEPLOY/看板三处） | 产品侧 | 可选；动 package.json 需裁定（不动则在 scripts/README.md 列命令表，推荐后者零风险） |
| Y13 | **洗护 XP +2 移入完成事务**（A4 伴生）：现走 60s 补偿任务，到账延迟 | 产品侧 | 建议不随本批（serviceStep.ts 归属交叉，等修复包落地后另行裁定） |

**超出 12 表位 3 范围的新发现，仅报备不入包**（除非老板/产品明示）：微光免费档 365 天有效期语义（32 档 O1）/商家端无差评展示位（差评提示仅在员工端店长视图）/退会后 rebate 账户 status 仍 active（32 档 P3-1）。

---

## 四、闸门映射（本包交付时跑法，备施工令引用）

| 闸门 | 命令（根目录） | 备注 |
|---|---|---|
| 三端 build | `npm run build`（单命令，tsc -b 内嵌类型检查） | 只许此一条（Y1 裁定） |
| server typecheck | `npm run typecheck -w @philia/server` | 无根 script，直跑 |
| smoke-routes | `node scripts/smoke-routes.mjs` | F1 口径随包补；fresh 环境需先造单或传 SMOKE_APPT_ID |
| smoke-deploy | `node scripts/smoke-deploy.mjs` | 73 项基线+本包新增断言 |
| check-nav-closure | `node scripts/check-nav-closure.mjs`（Node≥22；20/21 加 --experimental-websocket） | 本包若加新路由须双表申报（:63-134 路由表+smoke-routes anchors） |
| server e2e | `cd server && npm run test:e2e` | 临时库隔离；差评 −8 既有断言在 e2e.ts:1445-1456 |
| 客户端评价 e2e（新增） | `node apps/customer/scripts/review-e2e.mjs`（仿 me-page-e2e.mjs） | 随包新建 |
| 禁令 grep / 零新依赖零新迁移 | 照施工令 | B2 若走 0016 迁移则「零新迁移」口径需施工令豁免写明 |

**建议新路由**：本包预计零新路由（扣分录入若放商家端既有页内对话框则免申报；若新开页须双表申报）。

## 五、反对意见与风险（军规一）

1. **Y5 若选②（超阈值落 draft），本包性质从「前端为主修复包」变为「动 R12 钱链路」**——R12 刚双签归档，改动 execute 权限闸=重开钱链路验收，三方复走成本翻倍；建议除非老板明示，本批只走①。
2. **Y3 若只改步 5 一个词**：通知套两个异名（术前检查/家长确认）和客户端显示名留存，等于把同一个缺陷留到下一次走查再修一遍——归一的一次性成本低于分次。
3. **D2 若只做前端过滤不做 server 闸**：与 35 号档「防再上架闸门」精神不符——下架是状态位，前端过滤是展示层，店员误上架后收银台链路仍可能漏进；建议 server 侧结构性排除（Y8 推荐方案）。
4. **备料提前量的固有风险**：本清单基于 R11a 双签前快照；若 R11a 真机复走（件 4 注入/件 6 混合单/门店执行）出新 P1，范围以届时冻结任务书为准，本清单相应条目作废不重报。

## 六、自检记录（军规二）

- 查了什么：12 表 V3.3 位 3 全部 15 件逐件仓内定位（4 域并行侦察，累计命中文件 40+，关键结论全部带 文件:行号 双锚）；范围出处档案 8 件全读（24/19/19a/20/26/32/35/09+12）。
- 发现什么：走查报告外的**新坐实 6 件**——评价双入口且通知落缺陷入／步名异名 6 处+冻结表外异名 2 个／差评 −8 三跳过门径（寄养单永不扣分）／「完成服务+6」=寄养拆账非错分／draft 单无产生入口致驳回权空转／0015 纯 DDL 无种子。
- 改了什么：**零代码改动**（备料只读）；环境动作=还原 package-lock.json（npm install 再生残留）+删 smoke-shots/（B 窗核查截图残留）+经 gh API tarball 同步远端文档流至 `E:\tmp\remote-main\`（github.com:443 直连不通，git pull 待通道恢复补做）。
- 残留风险：生产库 xp_rules 现值只能复走实测（备料无法只读核实）；¥0 挂单来源待产品侧取证（Y10）；本地 git 与远端文档流差 8 个提交（纯 docs，代码面零差异已核）。
