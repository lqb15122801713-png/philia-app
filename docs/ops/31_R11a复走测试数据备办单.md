# R11a 复走测试数据备办单（八件）

> 产品侧牵头 · 2026-09-23（老板令：「牵头备测试数据八件，运营协同」）。
> 本件发给谁：老板（拍板遗留口径）/ QA 测试工程师（数据需求方，收件核对）/ 运营总监（协同执行）/ 项目经理（登记跟踪）/ 门店（真机造单执行）。
> 依据：《30_R11a会员骨架批复走用例包》§十二.8 八件需求（V1.0 冻结 58 条）；验收对象 main@65911fba（PR #40）。
> 编号注记：本件取 31 号，如撞号请项目经理改号并留痕。
> 纪律：备数据 ≠ 启动复走——复走启动令（QA/运营）照 CJ-0923-10② 待老板递达，本单只做备齐。
> V1.1（2026-09-23）：补 §六 件 4 注入执行包——老板 #14 拍板（CJ-0923-13）：按方案办+测试单真实收款（复走现场走 R12 链路统一退）+注入件保留标测试件不删。
> V1.2（2026-09-23）：**件 4 注入已执行（实证见 §6.5）**——期次撞车插曲修正（批次单①期次 2026-08→2026-07，见 §6.5 插曲留痕）；SSH 通道已建（老板授权 A 路）。

---

## 一、八件清单与备法

| # | 数据件 | 规格参数 | 备法 | 责任 | 状态 |
|---|---|---|---|---|---|
| 1 | 萤火付费号 1 个 | 萤火 ¥199/年，绑 1 宠，收银台到店付开通 | 真机收银台售卡造（内测期到店付口径，PR #40） | 门店/运营执行，产品侧远程支持 | 待备 |
| 2 | 烛光付费号 1 个 | 烛光 ¥299/年，绑 1 宠 | 同上 | 同上 | 待备 |
| 3 | 暖阳 4 宠号 1 个 | 暖阳 ¥599 + 第 4 只 +¥59 = **¥658**，绑 4 宠 | 同上（多宠附加费入单实证 QA40-A2-03） | 同上 | 待备 |
| 4 | 回馈金有余额且部分临期号 1 个 | 可用余额 ¥50（¥30 正常+¥20 临期≤30 天） | 产品侧注入实证（§六） | 产品侧 | ✅**已注入（2026-09-23，实证 §6.5）** |
| 5 | 无余额新微光号 1 个 | 客户端一键开微光，零余额 | 真机客户端注册（兼测 QA40-A1-01 注册 ≤30 秒计时） | 门店/运营 | 待备 |
| 6 | 可退混合单 1 笔 | 服务+商品一单结，商品行用回馈金抵扣混搭 | 真机收银台造单（用件 4 的号抵扣，复走现场退 → 实证 QA40-C5-01 退货按比例扣回接缝） | 门店/运营造单 | 待备（件 4 已就绪，解锁） |
| 7 | 可退纯商品单 1 笔 | 全价成犬粮 ¥129 在库 ✓（06 表 B2-1④） | 真机造单 | 门店/运营 | 待备 |
| 8 | 可退寄养单 1 笔 | 按晚计费明细 | 真机造单——**冒烟寄养单规范沿用：跑完即取消释放槽**（部署期坑②教训留痕口径） | 门店/运营 | 待备 |

**顺序约束**：件 4 已先行完成 ✓（件 6 抵扣混搭依赖已解锁）；件 1/2/3/5/6/7/8 可并行。
**收款口径（CJ-0923-13 老板拍）**：到店付测试单**真实收款**，复走现场走 R12 链路统一退（顺带实证退款链路）。

## 二、件 4 注入实证专项（八件唯一硬点）

1. **为什么必须注入**：回馈金「统一次月到账」（CJ-0922-13②）——当月任何消费只挂期次、不进可用余额；有效期 365 天——临期件只能靠有效期回填。自然路径两者都造不出。
2. **注入纪律**：
   - 走「注入实证」机制（军规口径：定时器/长周期类=监理代码级核查+产品侧注入实证，QA 不背）；
   - 只造 rebate 域测试行（rebate_accounts 余额 + rebate_logs 发放类流水：前后余额+单号+期次齐全），**不动已收/不动储值/不动 XP**——注入不是动账；
   - 注入流水单号统一 `INJECT-` 前缀 + 测试手机号段统一报备，防与真实会员混淆；
   - 注入件清单同步监理 B 窗，复走后由 B 窗核消确认。
3. **处置口径（老板已拍，CJ-0923-13）**：复走后注入件**保留留痕标测试件不删**（与三笔 5 星假评价保留先例 CJ-0922-10① 同轨）。

## 三、协同分工

- **产品侧（我）**：件 4 注入方案+执行+留痕（✅已完成）；提供内测期到店付留痕样例（应 QA §十二.3：收银台收款留痕截图样例，防「开通确认点了但没付钱」第三态）；测试手机号段报备（§六）。
- **运营总监**：组织门店真机造单（件 1/2/3/5/6/7/8），排期与复走窗口对齐。
- **QA**：收件核对——八件齐=具备开测条件（启动令仍等老板）；判定纪律照 30 号档。
- **门店**：真机执行，产品侧远程支持。
- **项目经理**：登记本单、跟踪八件状态。

## 四、自检记录（军规二 · 双件套之二）

- **Loop 1（草稿）**：照 30 号档 §十二.8 逐件映射 → 发现件 4「有余额且部分临期」与「统一次月到账+365 天有效期」冲突（自然造不出）→ 改注入实证方案。
- **Loop 2（修订）**：①八件与用例号映射核对（件 1/2/3→A2-01/02/03+B1/B2 区；件 4→C 区+D 区；件 5→A1-01；件 6/7/8→C5-01 接缝+R12 回归）✓；②寄养单槽位纪律核对（部署期坑②：冒烟寄养单占槽→跑完即取消释放槽）✓；③临期定义对齐 QA40-C3-02（有效期 365 天，临期=≤30 天，可证明细展示与到期口径）✓；④核查是否越权启动复走——本单只备数据 ✓。
- **Loop 3（执行后回检）**：①首跑撞期次唯一索引（§6.5 插曲）——批次单①改期次 2026-07 复跑，INSERT OR IGNORE 幂等实证（重跑零副作用）✓；②注入后实证 SELECT 全对（余额勾稽 0→3000→5000=5000 分=¥50.00）✓；③结算扫入风险复核——两条注入流水 settlement_id 均已回标，settleMonthly 不会扫入双入账 ✓；④注入脚本已从容器与主机清除 ✓。
- **残留风险**：①注入件有污染生产统计口径的风险——号段+前缀双保险+B 窗核消，处置口径已定（保留标测试件，CJ-0923-13）；②到店付测试单口径已定（真实收款+复走现场 R12 统一退，CJ-0923-13）；③注入明细 period 标当期「2026-09」为可见性刻意偏离（§6.1③），QA 看明细时注意期次文案与真实批次期次（2026-07/2025-09）的差异属预期。

## 五、反对意见与风险（军规一）

1. **件 6 的依赖被低估的风险**：混合单要用回馈金抵扣就必须等件 4 注入完成——**件 4 已注入（9-23），依赖已解锁**，门店造单顺序仍需运营盯（件 6 用 19900000004 号抵扣）。
2. **注入实证 vs「不直改生产数据」红线**：施工方红线「动规则不动账」不误伤本件——注入的是带标记的测试行且全留痕，属 07 军规认可的注入实证机制；但请 B 窗在复跑时把 INJECT- 前缀行列入核消清单，防测试行沉淀成「既成事实」。
3. **备齐≠达标**：八件只是开测条件，体验条款判定（注册 ≤30 秒/加载 <2 秒/零弹窗）仍以真机实测为准，数据备齐不代表 P0 过线。

---

## 六、件 4 注入执行包（V1.1 增补，CJ-0923-13 口径；V1.2 已执行）

### 6.1 设计依据（schema+代码实证核对，非拍脑袋）

已逐行核对 `server/drizzle/0015_unusual_ares.sql` + `server/src/services/rebate.ts` + `server/src/routers/membership.ts`：

1. **rebate_logs 无独立有效期列**——有效期=发放/入账时间 + `rebate_validity_days`（读表默认 365，plans 端点透出）→ **临期笔=入账时间回填**（2025-10-05 入账 → 2026-10-05 到期，距今 12 天=临期 ✓）。
2. **已到账余额**=rebate_accounts.balance_fen + 入账行 settlement_id 非空——注入行**必须回标 settlement_id**，否则被 settleMonthly 未来结算扫入双入账、且误入「本期预计」（balanceOf 的 pendingFen=settlement_id IS NULL 的 grant 合计）。
3. **明细可见性**：my 端点「本期明细」=当期 period 过滤（rebatePeriodOf：9-23 → '2026-09'）——注入流水 period 标当期 '2026-09' **仅为页面可见性**（测试件对真实期次的刻意偏离，批次单行保留真实期次，B 窗按 INJECT- 前缀核消即可识别）。
4. **会员实例**：memberships 行照 sell 端点同构（plan_key='plan_yinghuo'/sold_store=首店/started=now−30d/expires=now+335d/status='active'/paid_fen=19900）；users 旁路建档同构（kimi_id='phone:<手机号>'，nickname+user_roles customer）。
5. **幂等**：全量 `INSERT OR IGNORE`，重跑零副作用（执行期已实证——首跑部分入列后复跑无重复）。

### 6.2 注入 SQL（SQLite/libsql，VPS 容器内 philia.db）——**已执行版本**（批次单①期次=2026-07）

```sql
-- ===== R11a 复走件 4：回馈金有余额且部分临期测试号 =====
-- 口径：CJ-0923-13（保留标测试件不删）；测试号段报备：1990000 000x（件4=19900000004）
-- 幂等：INSERT OR IGNORE，重跑零副作用；余额勾稽：3000+2000=5000 分=¥50.00

INSERT OR IGNORE INTO users (id, kimi_id, nickname, phone, created_at, updated_at)
VALUES ('INJECT-USER-0004', 'phone:19900000004', '测试·回馈金临期号', '19900000004',
        unixepoch('now'), unixepoch('now'));

INSERT OR IGNORE INTO user_roles (id, user_id, role, created_at, updated_at)
VALUES ('INJECT-ROLE-0004', 'INJECT-USER-0004', 'customer', unixepoch('now'), unixepoch('now'));

INSERT OR IGNORE INTO memberships (id, user_id, plan_key, sold_store_id, started_at, expires_at,
                                   status, pet_count, paid_fen, created_at, updated_at)
VALUES ('INJECT-MEM-0004', 'INJECT-USER-0004', 'plan_yinghuo',
        (SELECT id FROM stores ORDER BY created_at LIMIT 1),
        unixepoch('now','-30 days'), unixepoch('now','+335 days'),
        'active', 1, 19900, unixepoch('now','-30 days'), unixepoch('now','-30 days'));

INSERT OR IGNORE INTO rebate_accounts (id, user_id, balance_fen, status, created_at, updated_at)
VALUES ('INJECT-ACC-0004', 'INJECT-USER-0004', 5000, 'active',
        unixepoch('now','-30 days'), unixepoch('now'));

-- 批次单①：正常笔（期次 2026-07，2026-08-05 次月 5 日到账——真实期次留存；
--         注：期次 2026-08 已被生产自检占位批次占用（uq_rebate_settlements_period），故用 2026-07）
INSERT OR IGNORE INTO rebate_settlements (id, period, granted_count, granted_fen, scheduled_day,
                                          executed_at, status, note, created_at, updated_at)
VALUES ('INJECT-SET-0001', '2026-07', 1, 3000, 5, unixepoch('2026-08-05 02:00:00'), 'done',
        'INJECT 测试件（CJ-0923-13）：复走件4 正常批次',
        unixepoch('2026-08-05 02:00:00'), unixepoch('2026-08-05 02:00:00'));

-- 批次单②：临期笔（期次 2025-09，2025-10-05 到账 → 2026-10-05 到期，距今 12 天=临期）
INSERT OR IGNORE INTO rebate_settlements (id, period, granted_count, granted_fen, scheduled_day,
                                          executed_at, status, note, created_at, updated_at)
VALUES ('INJECT-SET-0002', '2025-09', 1, 2000, 5, unixepoch('2025-10-05 02:00:00'), 'done',
        'INJECT 测试件（CJ-0923-13）：复走件4 临期批次',
        unixepoch('2025-10-05 02:00:00'), unixepoch('2025-10-05 02:00:00'));

-- 流水①：+¥30.00 正常笔（settlement_id 已回标：不进本期预计、不被 settleMonthly 扫入；
--         period 标当期 '2026-09' 仅为会员页本期明细可见——测试件刻意偏离，见 6.1③）
INSERT OR IGNORE INTO rebate_logs (id, user_id, account_id, type, delta_fen, before_fen, after_fen,
                                   source_id, period, settlement_id, note, created_at, updated_at)
VALUES ('INJECT-LOG-0004A', 'INJECT-USER-0004', 'INJECT-ACC-0004', 'grant', 3000, 0, 3000,
        'INJECT-SET-0001', '2026-09', 'INJECT-SET-0001',
        'INJECT 测试件：期次 2026-07 结算入账（批次 INJECT-SET-0001，次月到账口径）',
        unixepoch('2026-08-05 02:00:01'), unixepoch('2026-08-05 02:00:01'));

-- 流水②：+¥20.00 临期笔（入账 2025-10-05 + 365 天 → 2026-10-05 到期=临期）
INSERT OR IGNORE INTO rebate_logs (id, user_id, account_id, type, delta_fen, before_fen, after_fen,
                                   source_id, period, settlement_id, note, created_at, updated_at)
VALUES ('INJECT-LOG-0004B', 'INJECT-USER-0004', 'INJECT-ACC-0004', 'grant', 2000, 3000, 5000,
        'INJECT-SET-0002', '2026-09', 'INJECT-SET-0002',
        'INJECT 测试件：期次 2025-09 结算入账（批次 INJECT-SET-0002，临期笔 2026-10-05 到期）',
        unixepoch('2025-10-05 02:00:01'), unixepoch('2025-10-05 02:00:01'));
```

### 6.3 执行方式（实际执行记录）

SSH 直连 VPS（本机公钥已挂 ubuntu+root，老板授权 A 路）→ `docker cp` 进容器 → 容器内 node + @libsql/client 执行（philia.db）。注入脚本与探针脚本执行后已从容器/主机清除。

### 6.4 核消清单（B 窗复走时核消；回滚 SQL 备而不用——CJ-0923-13=保留标测试件）

注入件 8 行：`INJECT-USER-0004` / `INJECT-ROLE-0004` / `INJECT-MEM-0004` / `INJECT-ACC-0004` / `INJECT-SET-0001` / `INJECT-SET-0002` / `INJECT-LOG-0004A` / `INJECT-LOG-0004B`。

```sql
-- 回滚（仅老板改裁定时使用；当前口径=保留不删）
DELETE FROM rebate_logs WHERE id LIKE 'INJECT-LOG-0004%';
DELETE FROM rebate_settlements WHERE id LIKE 'INJECT-SET-%';
DELETE FROM rebate_accounts WHERE id='INJECT-ACC-0004';
DELETE FROM memberships WHERE id='INJECT-MEM-0004';
DELETE FROM user_roles WHERE id='INJECT-ROLE-0004';
DELETE FROM users WHERE id='INJECT-USER-0004';
```

### 6.5 执行留痕（已回填）

| 项 | 内容 |
|---|---|
| 执行人 / 时间 | 产品侧（Kimi Work 产品经理窗）· 2026-09-23 19:15（北京时间） |
| 执行通道 | SSH 直连（本机新生成专用密钥对，公钥挂 VPS ubuntu+root——老板授权 A 路，OrcaTerm 网页终端一行命令落地，私钥不出本机） |
| 执行输出 | `INJECT OK` + 实证 SELECT（见下） |
| 会员页目检 | 待 QA 真机（dev-login 手机号 19900000004，口令门同内测口径） |
| B 窗核消 | 待复走后核消 |

**实证 SELECT 输出（原文）**：

```
LOGS=[{"id":"INJECT-LOG-0004A","type":"grant","delta_fen":3000,"before_fen":0,"after_fen":3000,"period":"2026-09","settlement_id":"INJECT-SET-0001"},
      {"id":"INJECT-LOG-0004B","type":"grant","delta_fen":2000,"before_fen":3000,"after_fen":5000,"period":"2026-09","settlement_id":"INJECT-SET-0002"}]
VERIFY=[{"phone":"19900000004","plan_key":"plan_yinghuo","status":"active","paid_fen":19900,"balance_fen":5000,"acc_status":"active"}]
INJECT OK
```

**插曲留痕（诚信记录）**：首跑 FK 约束失败——批次单①期次 2026-08 撞 `uq_rebate_settlements_period` 唯一索引（生产自检已产生 2026-08 占位批次「本期无回馈金发放」），OR IGNORE 跳过该批次行 → 流水①回标落空 FK 报错中断。修正=批次单①改期次 2026-07（2026-08-05 到账）复跑成功；幂等实证：重跑已入列行零副作用。教训：注入件设计期次前先查 rebate_settlements 现有期次（已入自检 Loop 3）。

> 另报备：件 4 号客户端登录方式=dev-login 手机号 19900000004（口令门同既有内测口径，与演示账号同法）——QA 真机执行若登录不通，标「待实测」回流我补通道口径。
