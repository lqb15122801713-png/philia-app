# 经营参数硬编码排查清单（切换日前必清 · CJ-0925-10② 纲领落地）

> 产品侧 2026-09-26。档号=PD-06。依据：CJ-0925-10②（《经营参数硬编码排查清单》——凡经营数值/规则/文案入端口或槽位，切换日前必清；经营参数类争议以后不再呈老板，端口化即可）+ CJ-0923-16/17（凡内容皆留口/活的系统）。
> 方法：全仓扫描（server/src+apps+packages，362 命中人工复核）+ 既有端口台账（18 号档）逐域对账。
> 本件发给谁：老板（备查）/ 项目经理（登记+切换日闸门挂钩）/ A 窗（清理施工依据）/ 监理 B 窗（核查清单）。
> 纪律：本清单=切换日执行包前置闸——**切换日前全部「待清」项必须清零或经豁免登记**。

---

## 一、已端口化（合规，勿动）

| 域 | 参数 | 端口 | 状态 |
|---|---|---|---|
| 会员档 | 四档价/回馈比例/服务折扣/多宠规则/到账日/有效期 | 会员档页签（member_plans，PR #40） | ✅ |
| 回馈金 | 结算日/有效期/比例 | 同上（B2 域） | ✅ |
| 提成 | 全参数（含师徒 80/20、绩效系数） | 提成页签（PR #38，D1） | ✅ |
| XP | 全分值（含差评 −8）/日上限 60 | XP 页签（PR #38，D2） | ✅ |
| 时长引擎 | 犬猫×服务×体型×毛长系数 | 时长页签（duration_rules，PR #38） | ✅ |
| 退款阈值 | ¥500（threshold_fen=50000） | refund 域管道已通（refund_rules）——**页签缺口=PD-02 修复包 B1** | 🟡 管道通/页签缺 |
| 安心包 | 收费规则（全员免费）/内容物/效期预警 | C2 端口随控制台批首批 | 🆕 立项待建 |
| 会员机制 | 档位上下线/权益清单 | C1 端口 | 🆕 立项待建 |

## 二、硬编码待清（切换日前必清 · 带实证锚）

| # | 参数/文案 | 现状实证（文件:行号） | 清法 | 归属批次 |
|---|---|---|---|---|
| H1 | **考勤围栏半径 300m** | `server/src/routers/attendance.ts:35` `GEOFENCE_RADIUS_M = 300` 常量 | 入 D3 考勤规则端口（围栏半径/容差/补卡上限） | 控制台端口批 |
| H2 | **班次比对容差 10min** | 同文件 :36 常量族 | 同上 | 同上 |
| H3 | **「365 天」界面文案 6 处** | `apps/customer/src/pages/MemberOpenPage.tsx:140/:202`；`apps/merchant/src/components/cashier/MembershipPanel.tsx:157/:174/:437/:486` | 文案键化（读 membership_validity_days 插值）——A4 文案端口纪律 | 修复包（P2-5 在册） |
| H4 | **寄养默认容量 1 / 洗护槽默认容量 2** | `server/src/routers/appointment.ts:145/:135`（DEFAULT_BOARDING_ROOM_COUNT/DEFAULT_SLOT_CAPACITY） | 入 B6 寄养端口/B4 预约规则端口（房型间数） | 控制台端口批 |
| H5 | **注册默认档 plan_weiguang 硬编码** | `server/src/routers/membership.ts:341` 族（openFree/sell） | default_plan_key 配置一行——**修法已出（PD-05 件 2）** | 修复包 |
| H6 | **微光有效期语义** | 懒冻结对免费档生效（services/rebate.ts currentMembership） | 免费档豁免——**修法已出（PD-05 件 1）** | 修复包 |
| H7 | 权益墙/卡面经营性文案（权益名/副签/claim） | R11b 已文案键化（copy.ts 先例）——**其余三端界面经营性文案未键化** | 文案键+A4 文案端口（18 号档 A4 升级版） | 换皮批/端口批 |
| H8 | 安心包「¥15」注释残留 | `membership.ts:7`/`plans.tsx:9/:126` 注释（非显示面） | 注释级残留=豁免登记（改注释随修复包顺手，零风险） | 修复包顺手 |

## 三、豁免登记（硬编码但合理，不清）

- `seed.ts` 种子数值（档价 19900 等）：种子=首次灌库用途，运行期全部读表——**豁免**（注释已明）；
- e2e/冒烟断言值：测试件——豁免；
- schema 注释里的数值（设计注记）——豁免；
- 体验条款计时阈值（注册 30s 等）在 QA 用例包=判定尺，非系统参数——豁免。

## 四、纪律钉死（CJ-0925-10② 执行口径）

1. **「带端口出生」铁律扩展**：新批次验收强制检查升级——「可配参数入端口 + 界面文案入文案键 + 展示素材入槽位」三查（18 号档配套铁律①扩展）；
2. 经营参数类争议不再呈老板——端口化即可；端口默认值拿不准时，按冻结件/台账现值入，留痕可查；
3. 本清单=切换日执行包前置闸：切换日择日前 PM 核「待清=0 或豁免在案」。

## 五、自检记录（军规二）

- 扫描 362 命中→人工复核剔假阳（测试/注释/豁免族）→待清 8 件全部带 文件:行号 实证锚；与 18 号档端口台账逐域对账（已端口化域不误列待清）✓。
- 残留风险：文案域（H7）排查靠语义判断，可能漏边角——换皮批施工时 A 窗顺手登记新发现回本清单（活清单）。
