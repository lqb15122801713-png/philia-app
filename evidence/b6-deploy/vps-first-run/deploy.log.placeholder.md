# deploy.log.placeholder（占位说明）

产品侧持有的 VPS 首验运行日志 **`/mnt/agents/output/vps-first-run-deploy.log` 待移交**。

- 移交到位后：原样替换本占位文件为 `deploy.log` 入档（**不加工、不节选**，保持现场原貌）；
- 档案解读见同目录 `first-run-notes.md`（三缺陷现场 / 临时补丁 / 解堵后实证）；
- 本目录（evidence/b6-deploy）不入库；若后续任务 D 要求入证据分支，
  以 push 前 `git ls-files` 核对入档清单为准（.log 拦截教训）。
