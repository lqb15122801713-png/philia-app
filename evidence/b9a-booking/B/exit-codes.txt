# 批次 9a 任务 B · 全绿闸门退出码（2026-09-15，feat/b9a-booking 工作副本）

| 闸门 | 命令 | 退出码 |
|---|---|---|
| 三端生产构建 | npm run build（根，customer+merchant+staff） | 0 |
| 生产构建路由冒烟 | node scripts/smoke-routes.mjs（30/30，含 / 与 /booking/success 新增路由） | 0 |
| 服务端类型检查 | npm run typecheck -w @philia/server（tsc --noEmit） | 0 |
| 客户端类型检查 | tsc -b（apps/customer） | 0 |
| 服务端既有 e2e | npm run test:e2e -w @philia/server（独立临时库，全链路验收全部通过） | 0 |
| FFAAA5 自查 | grep -ri "FFAAA5" --include="*.ts*" apps/ | 0 命中 |
| 本批新增 text-white | grep "text-white" 本批改动/新增 6 文件 | 0 命中 |

日志：build.log / server-typecheck.log / server-e2e.log / smoke-routes.json（本目录）。
备注：server-e2e.log 尾部有一条临时目录 rm 的 EPERM 警告（Windows 文件锁，清理阶段），
不影响验收结论——脚本最终输出「全链路验收全部通过 ✅」且退出码 0。
